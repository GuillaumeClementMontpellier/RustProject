//Attention, si cela depasse la longueur / largeur de l'ecran, sdl2 panic
pub const SCREEN_SIZE: (u32, u32) = (1200,600);

pub const SCENE_SIZE: (u32, u32) = (SCREEN_SIZE.0 / 2, SCREEN_SIZE.1);

pub const PI: f32 = std::f32::consts::PI;